import 'package:flutter/material.dart';

class ColorSelection{
  Color backgroundColor;
  Color iconColor;
  Color textColor;

  ColorSelection(this.backgroundColor, this.iconColor, this.textColor);
}